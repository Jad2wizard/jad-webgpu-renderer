import { Vector2 } from 'three';
export type Extent = {
    n: number;
    e: number;
    s: number;
    w: number;
};
export type Point = Vector2;
export type Color = [number, number, number, number];
