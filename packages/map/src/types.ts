import { Vector2 } from 'three'
export type { Color, Blending } from '@webgpu-gmap/renderer'

export type Extent = { n: number; e: number; s: number; w: number } //西南东北的经纬度

export type Point = Vector2
